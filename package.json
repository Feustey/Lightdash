{
  "name": "lightdash",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && trunk build --release",
    "dev": "cd frontend && trunk serve",
    "start": "cd frontend && trunk serve --release"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 